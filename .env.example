# Copy to .env and adjust. .env is git-ignored.
# SQL Server refuses to start if this fails its complexity rules:
# 8+ chars with three of upper / lower / digit / symbol.
MSSQL_SA_PASSWORD=Your_Strong_Passw0rd
