# Mobile client

React Native (Expo) customer app — arrives in **Phase 5**.

Consumes the same generated TypeScript API client as the web storefront, so a
breaking API change is a compile error here too. See `docs/ARCHITECTURE.md` ADR-14.
